"""
Like NPM (node package manager) in javascript, there is a package manager for python which is called *PYPI*

we can use pip install on command line

By installing Pythpn from python.ord or through the Anaconda distribution that comes with installation of *pip*

*pip* is simple way to download packages at our command line directly from the PyPi repository.

There are packages already created as it is highly popularized

Syntax:

pip install requests

pip install colorama

to use it
Python, 

from colorama import init
init()

from colorama import Fore

print(Fore.RED + "Some red text")

--> it print some red text in red color


python package for excel:

package name is : Python Excel

"""